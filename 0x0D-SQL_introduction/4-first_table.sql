-- Script creates a table called first_table in your MySQL server.
-- database name will be passed as an argument of the mysql command

-- create the table.
CREATE TABLE IF NOT EXISTS first_table (id INT,
       name VARCHAR(256));
